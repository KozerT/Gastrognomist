import { useState } from "react";

const useBusinessSearch = (term, location) => {
  const [businesses, setBusinesses] = useState([]);
  const [amountResults, setAmountResults] 
};

export default useBusinessSearch;
